node_modules/
dist/
build/

# Secrets — every .env stays local
.env
*.env
!*.env.example

# Desktop build output
desktop/dist/
download/

# OS noise
.DS_Store
Thumbs.db

# Server data (SQLite) — never commit a database
server/data/
*.db
*.db-wal
*.db-shm
