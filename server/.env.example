PORT=3000
LIVEKIT_API_KEY=APIxxxxxxxxxxxx
LIVEKIT_API_SECRET=replace-me
LIVEKIT_URL=wss://vox.80.74.28.181.nip.io:8446
# Where the server calls LiveKit's API (kick/ban). Default: LIVEKIT_URL over https.
# LIVEKIT_API_URL=http://127.0.0.1:7880
# SQLite lives here; back up this folder and you've backed up the server.
DATA_DIR=./data
# Channels created on first setup; afterwards they are managed in the app.
ROOMS=general,games,music
TOKEN_TTL=10m
# Days a login stays remembered; renewed while it is being used.
SESSION_DAYS=90
# Optional: fix the first-run setup code (otherwise it is printed to the log).
# SETUP_CODE=ABCD-EFGH
DESKTOP_VERSION=0.2.0
